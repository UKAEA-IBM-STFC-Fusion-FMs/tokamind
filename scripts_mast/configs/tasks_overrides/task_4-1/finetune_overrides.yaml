# Task-specific overrides for finetuning.
#
# Model source is now specified via CLI:
#   python run_finetune.py --task task_4-1 --model MODEL [--tag TAG]
#
# This file contains only task-specific settings:
#   - task-specific training knobs (epochs, lr, batch_size, etc.)
#   - per-signal collate drop rates or masking policies
#   - preprocess settings specific to this task

preprocess:
  valid_windows:
     window_stride_sec: 0.02

loader:
  batch_size: 256    # <- task specific

# train:
#   stages:
#     - name: ft_heads
#       epochs: 30

#       scheduler:
#         grad_accum_steps: 2     # <- task specific

#       freeze:
#         token_encoder: true     # <- task specific
#         backbone: true
#         modality_heads: false
#         output_adapters: false

#       optimizer:
#         lr:
#           token_encoder: 0.0    # <- task specific
