# Use this file to override DCT3D tuning parameters from the shared embeddings.yaml.
# It is merged at load time when --emb_profile dct3d is passed to the entry script.
#
# embeddings.role_mode (finetune only):
#   tune   - tune DCT3D coefficients in this run
#   source - inherit DCT3D coefficients from the source run
#   config - use config/profile defaults without tuning or source artifacts
#
# Typical uses:
#   - Re-tune specific roles while inheriting others from the source model
#   - Tighten or relax explained-variance thresholds per role
#   - Change max_budget to control embedding dimensionality
#   - Increase n_shots / max_windows for more thorough tuning
#   - Disable guardrails for signals with unusual DCT profiles
#
# Example - re-tune only output, inherit input/actuator from source model:
#   embeddings:
#     role_mode:
#       input: source
#       actuator: source
#       output: tune
#
# Example - full re-tune with custom thresholds:
#   embeddings:
#     role_mode:
#       input: tune
#       actuator: tune
#       output: tune
#     tuning:
#       objective:
#         thresholds:
#           output: 0.990
#         max_budget:
#           output: 2048
#
# DO NOT store pre-tuned coeff_indices or per_signal_overrides here.
# Tuned indices are generated at training time and saved to
# runs/<run_id>/embeddings/ alongside model weights.

#embeddings:
#  tuning:
#    objective:
#      max_budget:
#        output: 9216