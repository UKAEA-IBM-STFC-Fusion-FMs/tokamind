# Task-specific overrides for evaluation.
#
# Model to evaluate is now specified via CLI:
#   python run_eval.py --task _test --model MODEL
#
# Eval results are automatically saved in: runs/<model_id>/eval/
#
# This file contains only task-specific eval settings:
#   - metrics/traces settings
#   - preprocess.valid_windows thresholds
#   - eval-specific data selection

eval:
  drop:
    inputs: # ["pf_active-coil_current"]
    actuators:
    outputs: # ["pf_active-solenoid_current", "summary-ip"]

#  traces:
#    enable: false
#    n_max: 2
#    signals: # ["pf_active-coil_current"]           # null → save all outputs
#    times_indexes: # [0, 1]                         # null → save full horizon
