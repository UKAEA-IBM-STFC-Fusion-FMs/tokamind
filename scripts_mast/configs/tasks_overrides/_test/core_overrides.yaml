# This file defines task-wide overrides that apply to *all* phases
# (pretrain / finetune / eval / tune_dct3d), e.g. model adapter sizes.
#
# Notes:
# - Task selection is determined by the enclosing folder name (`tasks_overrides/<task>/`).
# - The baseline/local task definition is inferred from that task name
#   (no `task_config:` pointers).
# - Keep this file minimal; prefer phase-specific changes in `<phase>_overrides.yaml`.



#model:
#  output_adapters:
#    # Default hidden size for output adapters (0 = linear head)
#    hidden_default: 0
#
#    # Task-specific per-signal overrides
#    hidden_overrides:
#      equilibrium-psi: 64
