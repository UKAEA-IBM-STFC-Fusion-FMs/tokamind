# test task definition.
# This "task" is implemented via a benchmark-style task_config living in
# scripts_mast/configs/local_tasks_def/.
#
# This file defines:
#   - the task name (must match the enclosing folder name)
#     (the task config is inferred *only* from `task`)
#   - optional task-wide overrides that should apply to *all* phases
#     (pretrain / finetune / eval / tune_dct3d), e.g. model adapter sizes.


task: _test

#model:
#  output_adapters:
#    # Default hidden size for output adapters (0 = linear head)
#    hidden_default: 0
#
#    # Task-specific per-signal overrides
#    hidden_overrides:
#      equilibrium-psi: 64
